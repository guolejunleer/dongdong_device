package com.dongdong.db.gen;

import android.database.Cursor;
import android.database.sqlite.SQLiteStatement;

import org.greenrobot.greendao.AbstractDao;
import org.greenrobot.greendao.Property;
import org.greenrobot.greendao.internal.DaoConfig;
import org.greenrobot.greendao.database.Database;
import org.greenrobot.greendao.database.DatabaseStatement;

import com.dongdong.db.entry.RoomIndexBean;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "ROOM_INDEX_BEAN".
*/
public class RoomIndexBeanDao extends AbstractDao<RoomIndexBean, Long> {

    public static final String TABLENAME = "ROOM_INDEX_BEAN";

    /**
     * Properties of entity RoomIndexBean.<br/>
     * Can be used for QueryBuilder and for referencing column names.
     */
    public static class Properties {
        public final static Property Id = new Property(0, Long.class, "id", true, "_id");
        public final static Property RoomId = new Property(1, int.class, "roomId", false, "ROOM_ID");
        public final static Property CardIndex = new Property(2, int.class, "cardIndex", false, "CARD_INDEX");
    }


    public RoomIndexBeanDao(DaoConfig config) {
        super(config);
    }
    
    public RoomIndexBeanDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(Database db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"ROOM_INDEX_BEAN\" (" + //
                "\"_id\" INTEGER PRIMARY KEY AUTOINCREMENT ," + // 0: id
                "\"ROOM_ID\" INTEGER NOT NULL ," + // 1: roomId
                "\"CARD_INDEX\" INTEGER NOT NULL );"); // 2: cardIndex
    }

    /** Drops the underlying database table. */
    public static void dropTable(Database db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"ROOM_INDEX_BEAN\"";
        db.execSQL(sql);
    }

    @Override
    protected final void bindValues(DatabaseStatement stmt, RoomIndexBean entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
        stmt.bindLong(2, entity.getRoomId());
        stmt.bindLong(3, entity.getCardIndex());
    }

    @Override
    protected final void bindValues(SQLiteStatement stmt, RoomIndexBean entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
        stmt.bindLong(2, entity.getRoomId());
        stmt.bindLong(3, entity.getCardIndex());
    }

    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
    }    

    @Override
    public RoomIndexBean readEntity(Cursor cursor, int offset) {
        RoomIndexBean entity = new RoomIndexBean();
        readEntity(cursor, entity, offset);
        return entity;
    }
     
    @Override
    public void readEntity(Cursor cursor, RoomIndexBean entity, int offset) {
        entity.setId(cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0));
        entity.setRoomId(cursor.getInt(offset + 1));
        entity.setCardIndex(cursor.getInt(offset + 2));
     }
    
    @Override
    protected final Long updateKeyAfterInsert(RoomIndexBean entity, long rowId) {
        entity.setId(rowId);
        return rowId;
    }
    
    @Override
    public Long getKey(RoomIndexBean entity) {
        if(entity != null) {
            return entity.getId();
        } else {
            return null;
        }
    }

    @Override
    public boolean hasKey(RoomIndexBean entity) {
        return entity.getId() != null;
    }

    @Override
    protected final boolean isEntityUpdateable() {
        return true;
    }
    
}
